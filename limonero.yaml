limonero:
    debug: true
    environment: dev
    secret: "123456"
    servers:
        database_url: mysql+pymysql://ericksunclair:senha123@localhost:3306/limonero
    services:
    config:
        SQLALCHEMY_POOL_SIZE: 10
        SQLALCHEMY_POOL_RECYCLE: 240